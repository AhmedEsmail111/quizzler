class Question {
  String questionText;
  bool questionAnswer;

  // constructor to assign these properties a value each time
  // I create an object out of this class

  Question(this.questionText, this.questionAnswer);
}
